# 0094: Native graveyard values

Uses sObjectMgr.GetClosestGraveYard() for the normal path and core-0012 read-only GraveYardMap access only for the existing alternate-graveyard search. Removes the foreign GraveyardManager architecture.

Ownership: **MOD**.
