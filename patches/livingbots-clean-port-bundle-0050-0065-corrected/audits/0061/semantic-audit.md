# Semantic audit — 0061

## Native group membership APIs

- Maps default legacy group checks to same-raid semantics and the explicit party/subgroup case to same-group semantics.
- Resolves unit targets through their player/owner so pet-owner buff/group behavior is preserved.

## Scope

Only the files listed in `changed-files.txt` are part of this patch. The patch is ordered after the preceding numbered patch in `APPLY_ORDER.txt`.
