# Semantic audit — 0057

## Native battleground join APIs

- Uses native battleground bracket calculation, team-index conversion and battlefield-port session handler.
- No battleground compatibility shim is introduced.

## Scope

Only the files listed in `changed-files.txt` are part of this patch. The patch is ordered after the preceding numbered patch in `APPLY_ORDER.txt`.
