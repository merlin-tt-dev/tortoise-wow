diff --git a/src/game/LFT/LFTMgr.cpp b/src/game/LFT/LFTMgr.cpp
index 85ad768..74ce6c0 100644
--- a/src/game/LFT/LFTMgr.cpp
+++ b/src/game/LFT/LFTMgr.cpp
@@ -5,6 +5,8 @@
 #include "Player.h"
 #include "World.h"
 
+#include <algorithm>
+
 LFTManager sLFTMgr;
 
 namespace
@@ -14,22 +16,22 @@ namespace
     std::vector<std::string> SplitPreserveEmpty(std::string const& value, char delimiter)
     {
         std::vector<std::string> parts;
-        std::string current;
+        parts.reserve(static_cast<size_t>(std::count(value.begin(), value.end(), delimiter)) + 1);
 
-        for (char c : value)
+        size_t start = 0;
+        while (true)
         {
-            if (c == delimiter)
-            {
-                parts.push_back(current);
-                current.clear();
-            }
-            else
+            size_t end = value.find(delimiter, start);
+            if (end == std::string::npos)
             {
-                current += c;
+                parts.emplace_back(value, start, value.size() - start);
+                break;
             }
+
+            parts.emplace_back(value, start, end - start);
+            start = end + 1;
         }
 
-        parts.push_back(current);
         return parts;
     }
 }
diff --git a/src/game/LFT/LFTQeueue.cpp b/src/game/LFT/LFTQeueue.cpp
index b18a074..499a793 100644
--- a/src/game/LFT/LFTQeueue.cpp
+++ b/src/game/LFT/LFTQeueue.cpp
@@ -22,22 +22,22 @@ namespace
     std::vector<std::string> SplitPreserveEmpty(std::string const& value, char delimiter)
     {
         std::vector<std::string> parts;
-        std::string current;
+        parts.reserve(static_cast<size_t>(std::count(value.begin(), value.end(), delimiter)) + 1);
 
-        for (char c : value)
+        size_t start = 0;
+        while (true)
         {
-            if (c == delimiter)
+            size_t end = value.find(delimiter, start);
+            if (end == std::string::npos)
             {
-                parts.push_back(current);
-                current.clear();
-            }
-            else
-            {
-                current += c;
+                parts.emplace_back(value, start, value.size() - start);
+                break;
             }
+
+            parts.emplace_back(value, start, end - start);
+            start = end + 1;
         }
 
-        parts.push_back(current);
         return parts;
     }
 
diff --git a/src/game/LFT/LFTUserGroups.cpp b/src/game/LFT/LFTUserGroups.cpp
index df16f39..fd4ae7b 100644
--- a/src/game/LFT/LFTUserGroups.cpp
+++ b/src/game/LFT/LFTUserGroups.cpp
@@ -16,22 +16,22 @@ namespace
     std::vector<std::string> SplitPreserveEmpty(std::string const& value, char delimiter)
     {
         std::vector<std::string> parts;
-        std::string current;
+        parts.reserve(static_cast<size_t>(std::count(value.begin(), value.end(), delimiter)) + 1);
 
-        for (char c : value)
+        size_t start = 0;
+        while (true)
         {
-            if (c == delimiter)
+            size_t end = value.find(delimiter, start);
+            if (end == std::string::npos)
             {
-                parts.push_back(current);
-                current.clear();
-            }
-            else
-            {
-                current += c;
+                parts.emplace_back(value, start, value.size() - start);
+                break;
             }
+
+            parts.emplace_back(value, start, end - start);
+            start = end + 1;
         }
 
-        parts.push_back(current);
         return parts;
     }
 
