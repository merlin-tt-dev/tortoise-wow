diff --git a/modules/mod-playerbots/src/playerbot/strategy/actions/AhAction.cpp b/modules/mod-playerbots/src/playerbot/strategy/actions/AhAction.cpp
index 19a831679b7a63bc05c950b9d1664867192185a6..fe78c6cfc76533ad14654346ce59fa9c73910dd7 100644
--- a/modules/mod-playerbots/src/playerbot/strategy/actions/AhAction.cpp
+++ b/modules/mod-playerbots/src/playerbot/strategy/actions/AhAction.cpp
@@ -9,6 +9,15 @@
 
 using namespace ai;
 
+namespace
+{
+uint32 GetAuctionItemCount(AuctionEntry const* auction)
+{
+    Item* item = auction ? sAuctionMgr.GetAItem(auction->itemGuidLow) : nullptr;
+    return item ? item->GetCount() : 0;
+}
+}
+
 bool AhAction::Execute(Event& event)
 {
     Player* requester = event.getOwner() ? event.getOwner() : GetMaster();
@@ -223,6 +232,10 @@ bool AhBidAction::ExecuteCommand(Player* requester, std::string text, Unit* auct
             if (auction->owner == bot->GetGUIDLow())
                 continue;
 
+            uint32 itemCount = GetAuctionItemCount(auction);
+            if (!itemCount)
+                continue;
+
             uint32 totalCost = std::min(auction->buyout, uint32(std::max(auction->bid, auction->startbid) * frand(1.05f, 1.25f)));
 
             usage = AI_VALUE2(ItemUsage, "item usage", ItemQualifier(auction).GetQualifier());
@@ -241,7 +254,7 @@ bool AhBidAction::ExecuteCommand(Player* requester, std::string text, Unit* auct
             case ItemUsage::ITEM_USAGE_AH:
             {
                 auto pmo = sPerformanceMonitor.start(PERF_MON_VALUE, "IsWorthBuyingFromAhToResellAtAH", ai);
-                bool isWorthBuyingFromAhToResellAtAH = ItemUsageValue::IsWorthBuyingFromAhToResellAtAH(sObjectMgr.GetItemPrototype(auction->itemTemplate), totalCost, auction->itemCount);
+                bool isWorthBuyingFromAhToResellAtAH = ItemUsageValue::IsWorthBuyingFromAhToResellAtAH(sObjectMgr.GetItemPrototype(auction->itemTemplate), totalCost, itemCount);
                 pmo.reset();
 
                 if (!isWorthBuyingFromAhToResellAtAH)
@@ -251,7 +264,7 @@ bool AhBidAction::ExecuteCommand(Player* requester, std::string text, Unit* auct
             }
             case ItemUsage::ITEM_USAGE_VENDOR:
                 //basically if AH price is lower than vendor sell price then it's worth it
-                if (totalCost / auction->itemCount >= (int32)sObjectMgr.GetItemPrototype(auction->itemTemplate)->SellPrice)
+                if (totalCost / itemCount >= (int32)sObjectMgr.GetItemPrototype(auction->itemTemplate)->SellPrice)
                     continue;
                 power = 1000;
                 break;
@@ -327,8 +340,11 @@ bool AhBidAction::ExecuteCommand(Player* requester, std::string text, Unit* auct
             freeMoney[ItemUsage::ITEM_USAGE_QUEST] = freeMoney[ItemUsage::ITEM_USAGE_AH] = freeMoney[ItemUsage::ITEM_USAGE_VENDOR] = freeMoney[ItemUsage::ITEM_USAGE_FORCE_NEED] = freeMoney[ItemUsage::ITEM_USAGE_FORCE_GREED] = (uint32)NeedMoneyFor::anything;
          
             ItemUsage usage = AI_VALUE2(ItemUsage, "item usage", ItemQualifier(auction).GetQualifier());
+            uint32 itemCount = GetAuctionItemCount(auction);
+            if (!itemCount)
+                continue;
 
-            std::string reason = ItemUsageValue::ReasonForNeed(usage, auction, auction->itemCount, bot);            
+            std::string reason = ItemUsageValue::ReasonForNeed(usage, auction, itemCount, bot);
 
             bidItems = BidItem(requester, auction, price, auctioneer, price == currentBuyoutPrice, reason);
 
@@ -372,8 +388,11 @@ bool AhBidAction::ExecuteCommand(Player* requester, std::string text, Unit* auct
             continue;
 
         uint32 cost = std::min(auction->buyout, uint32(std::max(auction->bid, auction->startbid) * frand(1.05f, 1.25f)));
+        uint32 itemCount = GetAuctionItemCount(auction);
+        if (!itemCount)
+            continue;
 
-        uint32 power = auction->itemCount;
+        uint32 power = itemCount;
         power *= 1000;
         power /= cost;
 
@@ -416,7 +435,9 @@ bool AhBidAction::BidItem(Player* requester, AuctionEntry* auction, uint32 price
 
     uint32 oldMoney = bot->GetMoney();
     ItemQualifier itemQualifier(auction);
-    uint32 count = auction->itemCount;
+    uint32 count = GetAuctionItemCount(auction);
+    if (!count)
+        return false;
 
     ItemPrototype const* proto = sObjectMgr.GetItemPrototype(auction->itemTemplate);
 
