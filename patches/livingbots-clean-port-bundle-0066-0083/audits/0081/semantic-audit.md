# Semantic audit 0081

## Native Meeting Stone taxi/transport APIs

Rebuilds TaxiFlightInterrupt semantics using native motion expiry, taxi-destination clearing and unmount; uses native Transport::AddPassenger(player).

Core impact: **none**. This patch is Mod-only.
