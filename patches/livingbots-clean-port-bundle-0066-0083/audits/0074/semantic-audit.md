# Semantic audit 0074

## Native stand, group and guild APIs

Uses GetStandState, native raid/DND names, and explicit guild-ID equality instead of a missing IsInGuild(guildId) helper.

Core impact: **none**. This patch is Mod-only.
