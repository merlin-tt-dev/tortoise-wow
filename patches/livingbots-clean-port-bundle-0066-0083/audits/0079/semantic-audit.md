# Semantic audit 0079

## Native skill unlearn APIs

Uses Penqle GetSkillRaceClassInfo and SetSkill(id, 0, 0), matching native skill-unlearn handling, instead of fork helpers GetSkillInfo/SetSkillStep.

Core impact: **none**. This patch is Mod-only.
