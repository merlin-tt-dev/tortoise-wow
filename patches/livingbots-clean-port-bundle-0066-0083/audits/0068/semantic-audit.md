# Semantic audit 0068

## Native master-looter GUID

Uses Group::GetLooterGuid(), the native Penqle master-loot authority, instead of the missing fork helper GetMasterLooterGuid().

Core impact: **none**. This patch is Mod-only.
